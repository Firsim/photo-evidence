# photo-evidence
ФОТОТАБЛИЦА — генератор доказательств для суда: создаёт документ Word из фото (JPEG/HEIC) с датой, местом и моделью камеры. Поддерживает iPhone, автоматически сортирует и геокодирует.
