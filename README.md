# ФОТОТАБЛИЦА — генератор доказательств для суда

📸 Инструмент для автоматического создания таблицы в формате Word из фотографий (включая HEIC с iPhone), с извлечением:
- Даты и времени съёмки
- Местоположения (через GPS → адрес)
- Модели камеры (например, iPhone 16 Pro)

Подходит для подготовки доказательств в гражданских и семейных делах.

> 💡 Для Windows можно скачать последнюю версию на странице: [Releases](https://github.com/Firsim/photo-evidence/releases)

---

## 🔧 Возможности

- Поддержка **JPEG и HEIC** (фото с iPhone)
- Автоматическое извлечение EXIF: дата, GPS, модель камеры
- Обратное геокодирование: координаты → читаемый адрес (на русском)
- Сортировка по дате: от старых к новым
- Миниатюры в документе
- Выходной файл: `!_ФОТОТАБЛИЦА_дата_время.docx` — сохраняется в папке с фото
- Простой запуск через `.bat`-файл — без знания Python

---

## 📂 Структура проекта

```
photo-evidence/
│
├── photo_evidence.py         ← основной скрипт
├── run.bat                   ← запуск с вводом пути
├── requirements.txt          ← список зависимостей
├── README.md                 ← эта инструкция
└── example_photo/            ← папка для теста (примеры HEIC и JPEG)
```

---

## 🚀 Быстрый старт (с примером)

1. Убедитесь, что установлен [Python 3.9+](https://www.python.org/downloads/)
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Запустите тест:
   ```bash
   run.bat
   ```
4. Когда запросит путь — введите:
   ```
   example_photo
   ```
5. В папке `example_photo` появится файл:
   ```
   !_ФОТОТАБЛИЦА_2025-11-01_14-30.docx
   ```

> 💡 Это ваш первый отчёт.

---

## ▶️ Как использовать со своими фото

1. Скопируйте папку `Мои фотографии` в удобное место.
2. Дважды кликните на `run.bat`.
3. Введите путь к папке с вашими фотографиями, например:
   ```
   D:\Мои фотографии
   ```
4. Подождите завершения — документ создастся автоматически.

---

## ⚠️ Важно

- На iPhone должна быть включена геолокация для Камеры:  
  **Настройки → Конфиденциальность → Геолокация → Камера → ВКЛ**
- Если фото сделаны через скриншот или мессенджер — EXIF может отсутствовать.
- Первый запуск требует интернета (для получения адресов по GPS).
- Для работы нужен только Python — всё остальное устанавливается автоматически.

---

## 🛠 Технические требования

- Python 3.9 или выше
- Windows / macOS / Linux (проверено на Windows)
- Свободное место: ~100 МБ
- Интернет (для геокодирования)

---

## 📦 Зависимости (`requirements.txt`)

```txt
Pillow>=10.0.0
pillow-heif>=0.16.0
python-docx>=0.8.11
geopy>=2.4.0
```

Установка зависимостей:
```bash
pip install -r requirements.txt
```

## 🖼️ Пример результата
![Скриншот результата](https://github.com/Firsim/photo-evidence/blob/709bfc2617a7b6bfb463a775da2107a9e853ba6b/example_screen.jpg)
